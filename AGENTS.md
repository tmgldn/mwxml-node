You are working on the task of converting this python MediaWiki XML parser to its node equivalent.

Use typescript, make it esm, npm publishable. It should have the same API albeit using TS/JS camelCase PascalCase conventions instead.

The project is https://github.com/mediawiki-utilities/python-mwxml and is local in ./python-mwxml

